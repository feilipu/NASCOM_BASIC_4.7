4   REM Used to check the hexloadr.bas file is properly loaded.5   REM This prints about the same as the input .bas file,6   REM good enough to do a diff (excl. white space) on anyway.10  for a = &hFF00 to &hFFFF20  print "poke", str$(a), ",", peek (a)30  next a104 REM Used to check your arbitrary HEX is properly loaded.105 REM this prints about the same as the input .hex file106 REM good enough to do an eyeball diff on anyway.110 for a = &hE000 to &hE03F120 print "peek", hex$(a), ", $", hex$(peek (a))130 next a