5  REM this prints about the same as the input .bas file,6  REM good enough to do a rough diff on anyway.10 let a = &hff0020 print "poke",str$(a),",",peek (a)30 a = a + 140 if a = 0 then end50 goto 20